def defaultArgumentfunction(x=20):
    print(" Value of x is ", x)


defaultArgumentfunction(10)
defaultArgumentfunction()   # if we did not give the argument value it will take 20 as argument value by default
                                                                                     # without throwing any error.